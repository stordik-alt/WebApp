import { useMemo, useRef, useState } from "react";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { toast } from "sonner";
import { Check, ImageUp, Loader2, Plus, Trash2, X } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { extractScreenshotStage, type OcrProduct, type OcrResult, type OcrHourlyMetric } from "@/lib/ocr.functions";
import { extractHourlyWithContext, type ProductProfileContext } from "@/lib/ocr.hourly.functions";
import { preprocessOcrImage } from "@/lib/ocr-image";
import { useProductNorms, useProducts, useShiftAggregates } from "@/lib/data";
import { currentNorm, findProductByCode, type Product } from "@/lib/products";
import { upsertImportedProductProfile } from "@/lib/productProfiles";
import { SHIFTS, type Employee } from "@/lib/metrics";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card } from "@/components/ui/card";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { useApprovalFields } from "@/lib/auth";

type DraftRow = { key: string; ocrName: string; employeeId: string | null; position: "HA" | "TUP"; oee: string; performance: string; availableTime: string; helpScore: string; confidence: number; include: boolean };
type DraftProduct = OcrProduct & { key: string; employees_per_product: number | null };
type ProductSetupDraft = { key: string; code: string; name: string; norm: string; capacity: string; confidence: number; counterpartCode: string; counterpartName: string; counterpartNorm: string; counterpartCapacity: string };
type ImportStage = "products" | "employees" | "hourly" | "ready";
const strip = (s: string) => s.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9 ]/g, "").trim();
function matchEmployee(name: string, employees: Employee[]): Employee | undefined { const n = strip(name); if (!n) return undefined; const exact = employees.find((e) => strip(e.full_name) === n); if (exact) return exact; const parts = n.split(/\s+/).filter(Boolean); return employees.find((e) => { const en = strip(e.full_name); return parts.length > 1 && parts.every((p) => en.includes(p)); }); }
function normalizeProfileCode(value: string | null | undefined): string { return (value ?? "").trim().toLowerCase().replace(/\s+/g, ""); }
function profileMatchesCode(profile: ProductProfileContext, code: string): boolean { const wanted = normalizeProfileCode(code); return normalizeProfileCode(profile.ha_subassy) === wanted || normalizeProfileCode(profile.tup_subassy) === wanted; }
function profileNormForCode(profile: ProductProfileContext, code: string): number | null { const wanted = normalizeProfileCode(code); if (normalizeProfileCode(profile.ha_subassy) === wanted && profile.h_norm_per_hour != null) return Number(profile.h_norm_per_hour); if (normalizeProfileCode(profile.tup_subassy) === wanted && profile.t_norm_per_hour != null) return Number(profile.t_norm_per_hour); return null; }

export function ScreenshotImport({ employees, onImported }: { employees: Employee[]; onImported?: () => void }) {
  const qc = useQueryClient(); const approval = useApprovalFields(); const extractStage = useServerFn(extractScreenshotStage); const extractHourlyContext = useServerFn(extractHourlyWithContext);
  const { data: products = [] } = useProducts(); const { data: norms = [] } = useProductNorms(); const { records: existingRecords, shifts: existingShifts } = useShiftAggregates();
  const fileRef = useRef<HTMLInputElement>(null);
  const [busy, setBusy] = useState(false); const [stage, setStage] = useState<ImportStage>("products"); const [result, setResult] = useState<OcrResult | null>(null); const [screenshotPath, setScreenshotPath] = useState<string | null>(null); const [previewUrl, setPreviewUrl] = useState<string | null>(null); const [workDate, setWorkDate] = useState(""); const [shift, setShift] = useState<string>(SHIFTS[0]); const [line, setLine] = useState(""); const [productCode, setProductCode] = useState(""); const [normValue, setNormValue] = useState(""); const [productDrafts, setProductDrafts] = useState<DraftProduct[]>([]); const [rows, setRows] = useState<DraftRow[]>([]);
  const [addedEmployees, setAddedEmployees] = useState<Employee[]>([]); const [employeeSetupOpen, setEmployeeSetupOpen] = useState(false); const [addEmployeeOpen, setAddEmployeeOpen] = useState(false); const [addEmployeeRowKey, setAddEmployeeRowKey] = useState<string | null>(null); const [addEmployeeName, setAddEmployeeName] = useState(""); const [addEmployeePersonalNo, setAddEmployeePersonalNo] = useState("");
  const [newProductModalOpen, setNewProductModalOpen] = useState(false); const [productSetupSaving, setProductSetupSaving] = useState(false); const [productSetupDrafts, setProductSetupDrafts] = useState<ProductSetupDraft[]>([]); const [createdProducts, setCreatedProducts] = useState<Product[]>([]); const [productIdName, setProductIdName] = useState(""); const [hourlyMetrics, setHourlyMetrics] = useState<OcrHourlyMetric[]>([]); const [productProfiles, setProductProfiles] = useState<ProductProfileContext[]>([]);
  const activeEmployees = useMemo(() => [...employees, ...addedEmployees].filter((e) => e.active), [employees, addedEmployees]);
  const allProducts = useMemo(() => { const byCode = new Map<string, Product>(); [...products, ...createdProducts].forEach((p) => byCode.set(p.code.trim().toLowerCase(), p)); return Array.from(byCode.values()); }, [products, createdProducts]);
  const normNum = normValue === "" ? null : Number(normValue); const missingProductCodes = useMemo(() => productDrafts.filter((p) => !findProductByCode(allProducts, p.product_code)).map((p) => p.product_code), [productDrafts, allProducts]);
  const patch = (key: string, p: Partial<DraftRow>) => setRows((prev) => prev.map((r) => (r.key === key ? { ...r, ...p } : r)));
  const reset = () => { setStage("products"); setResult(null); setHourlyMetrics([]); setProductProfiles([]); setEmployeeSetupOpen(false); setRows([]); setProductDrafts([]); setScreenshotPath(null); setPreviewUrl(null); setProductCode(""); setNormValue(""); setAddedEmployees([]); setAddEmployeeOpen(false); setAddEmployeeRowKey(null); setAddEmployeeName(""); setAddEmployeePersonalNo(""); setNewProductModalOpen(false); setProductSetupSaving(false); setProductSetupDrafts([]); setCreatedProducts([]); if (fileRef.current) fileRef.current.value = ""; };
  const openAddEmployee = (row: DraftRow) => { setAddEmployeeRowKey(row.key); setAddEmployeeName(row.ocrName.trim()); setAddEmployeePersonalNo(""); setAddEmployeeOpen(true); };
  const createEmployee = useMutation({ mutationFn: async () => { const fullName = addEmployeeName.trim(); const personalNo = addEmployeePersonalNo.trim(); if (!fullName) throw new Error("Zadejte jméno zaměstnance."); if (personalNo) { const { data: byPersonalNo, error: lookupError } = await supabase.from("employees").select("*").eq("personal_no", personalNo).maybeSingle(); if (lookupError) throw lookupError; if (byPersonalNo) return byPersonalNo as Employee; } const existing = matchEmployee(fullName, [...employees, ...addedEmployees]); if (existing) return existing; const payload = { full_name: fullName, personal_no: personalNo || null, qual_ha: false, qual_tup: false, active: true, is_temporary: false, position_type: "standard" as const, note: null }; const { data, error } = await supabase.from("employees").insert(payload).select("*").single(); if (error) throw error; if (!data) throw new Error("Zaměstnanec byl uložen, ale nepodařilo se načíst vytvořený záznam."); return data as Employee; }, onSuccess: (employee) => { setAddedEmployees((prev) => prev.some((e) => e.id === employee.id) ? prev : [...prev, employee]); if (addEmployeeRowKey) patch(addEmployeeRowKey, { employeeId: employee.id }); qc.invalidateQueries({ queryKey: ["employees"] }); setAddEmployeeOpen(false); setAddEmployeeRowKey(null); setAddEmployeeName(""); setAddEmployeePersonalNo(""); toast.success(`Zaměstnanec „${employee.full_name}" byl přidán a přiřazen k řádku importu.`); }, onError: (e: Error) => toast.error(`Zaměstnance se nepodařilo přidat: ${e.message}`) });
  const openProductSetup = (detected: DraftProduct[]) => { setProductIdName(""); const detectedCodes = new Set(detected.map((p) => p.product_code.trim().toLowerCase())); const missing = Array.from(new Set(detected.map((p) => p.product_code.trim()).filter(Boolean))).filter((code) => !findProductByCode(allProducts, code)); if (!missing.length) return; const detectedByCode = new Map(detected.map((p) => [p.product_code.trim().toLowerCase(), p])); setProductSetupDrafts(missing.map((code, i) => { const detectedProduct = detectedByCode.get(code.toLowerCase()); const isH = /^H_/i.test(code); const isT = /^T_/i.test(code); const oppositeDetected = isH ? Array.from(detectedCodes).some((x) => /^T_/i.test(x)) : isT ? Array.from(detectedCodes).some((x) => /^H_/i.test(x)) : false; return { key: i + "-" + code, code, name: "", norm: detectedProduct?.norm_per_hour != null ? String(detectedProduct.norm_per_hour) : "", capacity: "1", confidence: detectedProduct?.confidence ?? 0.5, counterpartCode: oppositeDetected ? "" : "", counterpartName: "", counterpartNorm: "", counterpartCapacity: "1" }; })); setNewProductModalOpen(true); };
  const createProductIds = async () => { if (!productSetupDrafts.length) return; setProductSetupSaving(true); try { const date = workDate || new Date().toISOString().slice(0, 10); const created: Product[] = []; const items: Array<{ code: string; name: string; norm: string; capacity: string; confidence: number; primary: boolean; sourceKey: string }> = []; const seen = new Set<string>(); for (const draft of productSetupDrafts) { const add = (code: string, name: string, norm: string, capacity: string, primary: boolean) => { const normalized = code.trim().toLowerCase(); if (!normalized || seen.has(normalized)) return; seen.add(normalized); items.push({ code: code.trim(), name: name.trim(), norm, capacity, confidence: draft.confidence, primary, sourceKey: draft.key }); }; add(draft.code, draft.name, draft.norm, draft.capacity, true); if (draft.counterpartCode.trim()) add(draft.counterpartCode, draft.counterpartName, draft.counterpartNorm, draft.counterpartCapacity, false); }
    for (const item of items) { const code = item.code; const norm = Number(item.norm); const capacity = Number(item.capacity); if (!code) throw new Error("Každý Product ID musí mít kód."); let product = findProductByCode(allProducts, code) ?? created.find((p) => p.code.trim().toLowerCase() === code.toLowerCase()); const existedBefore = Boolean(product); const variant = /^T_/i.test(code) ? "T" : "H"; if (!product) { if (!Number.isFinite(norm) || norm <= 0) throw new Error("Zadejte platnou normu pro " + code + "."); if (!Number.isInteger(capacity) || capacity < 1) throw new Error("Zadejte platnou kapacitu operátorů pro " + code + "."); const { data, error } = await supabase.from("products").insert({ code, name: item.name || productIdName.trim(), employees_per_product: capacity, first_seen_date: date, variant_type: variant, ...approval() }).select("*").single(); if (error) throw error; product = data as Product; } else if (item.primary && item.name) { const { data, error } = await supabase.from("products").update({ name: item.name, employees_per_product: Number.isInteger(capacity) && capacity >= 1 ? capacity : product.employees_per_product, variant_type: variant }).eq("id", product.id).select("*").single(); if (error) throw error; product = data as Product; } if (!existedBefore || item.primary) created.push(product); }
    const productByCode = new Map<string, Product>(); [...allProducts, ...created].forEach((p) => productByCode.set(p.code.trim().toLowerCase(), p)); const handledProfiles = new Set<string>(); for (const draft of productSetupDrafts) { const firstCode = draft.code.trim(); const firstItem = items.find((item) => item.code.toLowerCase() === firstCode.toLowerCase()); const counterpartCode = draft.counterpartCode.trim(); const counterpartItem = counterpartCode ? items.find((item) => item.code.toLowerCase() === counterpartCode.toLowerCase()) : undefined; const hCode = /^H_/i.test(firstCode) ? firstCode : counterpartCode; const tCode = /^T_/i.test(firstCode) ? firstCode : counterpartCode; const hItem = /^H_/i.test(firstCode) ? firstItem : counterpartItem; const tItem = /^T_/i.test(firstCode) ? firstItem : counterpartItem; const pairKey = `${(hCode || firstCode).toLowerCase()}|${(tCode || "").toLowerCase()}`; if (handledProfiles.has(pairKey)) continue; handledProfiles.add(pairKey); const hProduct = hCode ? productByCode.get(hCode.toLowerCase()) : undefined; const tProduct = tCode ? productByCode.get(tCode.toLowerCase()) : undefined; await upsertImportedProductProfile({ profileName: productIdName.trim() || firstItem?.name || counterpartItem?.name || null, ha: hProduct && hItem ? { code: hProduct.code, norm: Number(hItem.norm), capacity: Number(hItem.capacity) } : undefined, tup: tProduct && tItem ? { code: tProduct.code, norm: Number(tItem.norm), capacity: Number(tItem.capacity) } : undefined, validFrom: date }); }
    setCreatedProducts((prev) => { const byCode = new Map(prev.map((p) => [p.code.trim().toLowerCase(), p])); created.forEach((p) => byCode.set(p.code.trim().toLowerCase(), p)); return Array.from(byCode.values()); }); setProductDrafts((prev) => prev.map((p) => { const setup = productSetupDrafts.find((d) => d.code.trim().toLowerCase() === p.product_code.trim().toLowerCase()); return setup ? { ...p, norm_per_hour: Number(setup.norm), employees_per_product: Number(setup.capacity) } : p; })); setNewProductModalOpen(false); qc.invalidateQueries({ queryKey: ["products"] }); qc.invalidateQueries({ queryKey: ["product_profiles"] }); qc.invalidateQueries({ queryKey: ["product_norms"] }); toast.success(`Zkontrolováno / založeno ${created.length} Product ID. Pokračuji na zaměstnance.`); if (previewUrl) void runEmployeeStage(previewUrl); } finally { setProductSetupSaving(false); } };
  const runEmployeeStage = async (imageDataUrl: string) => { setBusy(true); setStage("employees"); try { const r = await extractStage({ data: { imageDataUrl, stage: "employees" } }); const { data: dbEmployees, error: employeeLoadError } = await supabase.from("employees").select("*").eq("active", true); if (employeeLoadError) throw employeeLoadError; const freshEmployees = (dbEmployees ?? []) as Employee[]; const employeePool = [...freshEmployees, ...addedEmployees]; setResult((prev) => ({ ...(prev ?? r), work_date: prev?.work_date ?? r.work_date, shift: prev?.shift ?? r.shift, line: prev?.line ?? r.line, product_code: prev?.product_code ?? r.product_code, norm_per_hour: prev?.norm_per_hour ?? r.norm_per_hour, products: prev?.products?.length ? prev.products : r.products, header_confidence: Math.max(prev?.header_confidence ?? 0, r.header_confidence), rows: r.rows, hourly_metrics: prev?.hourly_metrics ?? [], predicted_shift_output: prev?.predicted_shift_output ?? null, productive_minutes: prev?.productive_minutes ?? null })); const fallbackPosition: "HA" | "TUP" = productDrafts.some((p) => /^T_/i.test(p.product_code)) ? "TUP" : "HA"; const nextRows: DraftRow[] = r.rows.map((row, i) => { const emp = matchEmployee(row.employee_name, employeePool); return { key: i + "-" + (row.employee_name || "manual"), ocrName: row.employee_name, employeeId: emp?.id ?? null, position: row.position ?? fallbackPosition, oee: row.oee !== null ? String(row.oee) : "", performance: row.performance !== null ? String(row.performance) : "", availableTime: row.available_time !== null ? String(row.available_time) : "", helpScore: "0", confidence: row.confidence, include: true }; }); setRows(nextRows); if (!nextRows.length || nextRows.some((r) => r.include && !r.employeeId)) { setEmployeeSetupOpen(true); if (!nextRows.length) toast.warning("OCR zaměstnanců nevrátil žádný čitelný řádek. Hodinová data se zatím nespustí."); } else await runHourlyStage(imageDataUrl, nextRows); } catch (e) { toast.error("OCR zaměstnanců se nepodařilo dokončit: " + (e as Error).message); setStage("employees"); } finally { setBusy(false); } };
  const runHourlyStage = async (imageDataUrl: string, employeeRows: DraftRow[] = rows) => { setBusy(true); setStage("hourly"); try {
    const { data: profileRows, error: profileError } = await supabase.from("product_profiles").select("id,ha_subassy,h_capacity,h_norm_per_hour,tup_subassy,t_capacity,t_norm_per_hour");
    if (profileError) throw new Error(`Nepodařilo se načíst Product Profile: ${profileError.message}`);
    const profiles = (profileRows ?? []) as ProductProfileContext[];
    setProductProfiles(profiles);
    const matchedProfiles = profiles.filter((profile) => productDrafts.some((draft) => profileMatchesCode(profile, draft.product_code.trim())) && ((profile.h_norm_per_hour != null && profile.h_capacity != null) || (profile.t_norm_per_hour != null && profile.t_capacity != null)));
    const operatorCount = employeeRows.filter((row) => row.include).length;
    if (!operatorCount) throw new Error("3. sekvence nemůže běžet bez skutečného počtu operátorů z 2. sekvence.");
    if (!matchedProfiles.length) throw new Error("Pro rozpoznané Product ID nebyl nalezen Product Profile s normou a kapacitou.");
    const r = await extractHourlyContext({ data: { imageDataUrl, context: { profiles: matchedProfiles, operator_count: operatorCount } } });
    const hourly = r.hourly_metrics.map((metric) => ({ hour: metric.hour, product_code: metric.product_code, actual_output: metric.actual_output, performance_pct: metric.performance_pct, availability_pct: metric.availability_pct, norm_per_hour: metric.norm_per_hour }));
    setHourlyMetrics(hourly);
    const avgPerformanceValues = hourly.map((x) => x.performance_pct).filter((x): x is number => x !== null);
    const avgAvailabilityValues = hourly.map((x) => x.availability_pct).filter((x): x is number => x !== null);
    const performanceFallback = avgPerformanceValues.length ? avgPerformanceValues.reduce((a, b) => a + b, 0) / avgPerformanceValues.length : null;
    const availabilityFallback = avgAvailabilityValues.length ? avgAvailabilityValues.reduce((a, b) => a + b, 0) / avgAvailabilityValues.length : null;
    const completedRows = employeeRows.map((row) => ({ ...row, oee: r.actual_shift_oee_pct !== null ? String(Number(r.actual_shift_oee_pct.toFixed(2))) : row.oee, performance: row.performance === "" && performanceFallback !== null ? String(performanceFallback) : row.performance, availableTime: row.availableTime === "" && availabilityFallback !== null ? String(availabilityFallback) : row.availableTime }));
    setRows(completedRows);
    const primaryProfile = matchedProfiles[0];
    const primaryNorm = primaryProfile ? (primaryProfile.h_norm_per_hour ?? primaryProfile.t_norm_per_hour ?? null) : null;
    setResult((prev) => ({ ...(prev ?? { work_date: null, shift: null, line: null, product_code: null, norm_per_hour: null, products: [], header_confidence: 0, rows: [], hourly_metrics: [], predicted_shift_output: null, productive_minutes: null }), work_date: prev?.work_date ?? null, shift: prev?.shift ?? null, line: prev?.line ?? null, product_code: prev?.product_code ?? productDrafts[0]?.product_code ?? null, norm_per_hour: primaryNorm ?? prev?.norm_per_hour ?? null, products: prev?.products?.length ? prev.products : productDrafts.map(({ product_code, norm_per_hour, confidence }) => ({ product_code, norm_per_hour, confidence })), rows: completedRows, hourly_metrics: hourly, predicted_shift_output: r.predicted_shift_output, productive_minutes: hourly.length >= 3 ? 8 * 60 - 10 - 30 - 5 : null, raw: r.raw }));
    setStage("ready");
    toast.success(`OCR dokončen: ${hourly.length} hodin, skutečné OEE ${r.actual_shift_oee_pct !== null ? r.actual_shift_oee_pct.toFixed(2) + " %" : "nelze spočítat"}.`);
  } catch (e) { toast.error("OCR hodinových dat se nepodařilo dokončit: " + (e as Error).message); setStage("hourly"); } finally { setBusy(false); } };
  const onFile = async (file: File) => { setBusy(true); setStage("products"); setResult(null); setRows([]); setProductDrafts([]); setHourlyMetrics([]); try { const dataUrl = await new Promise<string>((res, rej) => { const fr = new FileReader(); fr.onload = () => res(String(fr.result)); fr.onerror = () => rej(new Error("Soubor se nepodařilo načíst.")); fr.readAsDataURL(file); }); setPreviewUrl(dataUrl); const ocrDataUrl = await preprocessOcrImage(dataUrl, { scale: 2, quality: 0.94, maxWidth: 4096, maxHeight: 4096 }); const ext = (file.name.split(".").pop() || "png").toLowerCase(); const path = "daily/" + new Date().toISOString().slice(0, 10) + "/" + crypto.randomUUID() + "." + ext; const { error: upErr } = await supabase.storage.from("screenshots").upload(path, file, { contentType: file.type || "image/png" }); if (upErr) toast.warning("Screenshot se nepodařilo uložit do archivu, rozpoznávání pokračuje."); else setScreenshotPath(path); const { data: profileRows, error: profileError } = await supabase.from("product_profiles").select("id,ha_subassy,h_capacity,h_norm_per_hour,tup_subassy,t_capacity,t_norm_per_hour"); if (profileError) throw new Error(`Nepodařilo se načíst Product Profile: ${profileError.message}`); setProductProfiles((profileRows ?? []) as ProductProfileContext[]); const r = await extractStage({ data: { imageDataUrl: ocrDataUrl, stage: "products" } }); setResult(r); setWorkDate(r.work_date ?? new Date().toISOString().slice(0, 10)); setShift(r.shift && SHIFTS.includes(r.shift as never) ? r.shift : SHIFTS[0]); setLine(r.line ?? ""); setProductCode(r.product_code ?? ""); setNormValue(r.norm_per_hour !== null ? String(r.norm_per_hour) : ""); const detectedProducts = r.products?.length ? r.products : (r.product_code ? [{ product_code: r.product_code, norm_per_hour: r.norm_per_hour, confidence: r.header_confidence }] : []); setProductDrafts(detectedProducts.map((p, i) => ({ ...p, key: i + "-" + p.product_code, employees_per_product: findProductByCode(allProducts, p.product_code)?.employees_per_product ?? null }))); const newProducts = detectedProducts.filter((p) => !findProductByCode(allProducts, p.product_code)); if (newProducts.length > 0) { setStage("products"); openProductSetup(detectedProducts); toast.warning("Nalezeno " + newProducts.length + " nové Product ID. Nejprve je založte."); } else await runEmployeeStage(ocrDataUrl); } catch (e) { toast.error((e as Error).message); setStage("products"); } finally { setBusy(false); } };
  const unresolvedEmployeeRows = rows.filter((r) => r.include && !r.employeeId); const continueAfterEmployees = async () => { if (!previewUrl) return; if (!rows.length) { toast.warning("Nejdříve musí OCR rozpoznat alespoň jednoho zaměstnance."); return; } if (unresolvedEmployeeRows.length) { toast.warning("Nejprve přiřaďte nebo vytvořte všechny zaměstnance z OCR."); return; } setEmployeeSetupOpen(false); const ocrDataUrl = await preprocessOcrImage(previewUrl, { scale: 2, quality: 0.94, maxWidth: 4096, maxHeight: 4096 }); await runHourlyStage(ocrDataUrl, rows); };
  const confirmImport = useMutation({ mutationFn: async () => { if (!line.trim()) throw new Error("Doplňte linku."); const l = line.trim().toLowerCase(); const includedRows = rows.filter((r) => r.include); const unresolved = includedRows.filter((r) => !r.employeeId); if (unresolved.length) throw new Error(`Nelze importovat: ${unresolved.length} ${unresolved.length === 1 ? "řádek nemá přiřazeného zaměstnance" : "řádky nemají přiřazeného zaměstnance"}. Přiřaďte nebo vytvořte zaměstnance.`); const selected = includedRows.filter((r) => r.employeeId && !existingRecords.some((x) => x.employee_id === r.employeeId && x.work_date === workDate && x.shift === shift && x.line.trim().toLowerCase() === l)); if (!selected.length) { const duplicateCount = includedRows.filter((r) => r.employeeId && existingRecords.some((x) => x.employee_id === r.employeeId && x.work_date === workDate && x.shift === shift && x.line.trim().toLowerCase() === l)).length; throw new Error(`Všechny vybrané řádky už jsou pro datum ${workDate}, směnu ${shift} a linku ${line.trim()} uložené (duplicitních řádků: ${duplicateCount}).`); }
    const drafts = productDrafts.filter((p) => p.product_code.trim()); if (!drafts.length && productCode.trim()) drafts.push({ key: "legacy", product_code: productCode.trim(), norm_per_hour: normNum, confidence: 0.5, employees_per_product: null }); if (!drafts.length) throw new Error("Screenshot neobsahuje žádný rozpoznaný produkt.");
    const productByCode = new Map<string, Product>(); const draftCodes = drafts.map((d) => d.product_code.trim()).filter(Boolean); const { data: freshProducts, error: freshProductsError } = await supabase.from("products").select("*").in("code", draftCodes); if (freshProductsError) throw freshProductsError; (freshProducts ?? []).forEach((p) => productByCode.set(p.code.trim().toLowerCase(), p as Product));
    for (const d of productSetupDrafts) { if (d.counterpartCode.trim()) { const { data: counterpart, error } = await supabase.from("products").select("*").eq("code", d.counterpartCode.trim()).maybeSingle(); if (error) throw error; if (counterpart) productByCode.set(counterpart.code.trim().toLowerCase(), counterpart as Product); } }
    if (missingProductCodes.length) throw new Error("Nejprve dokončete kontrolu/založení všech Product ID v prvním kroku.");
    const primary = productByCode.get(drafts[0].product_code.trim().toLowerCase()); const hCode = drafts.find((d) => /^H_/i.test(d.product_code))?.product_code?.trim().toLowerCase() || productSetupDrafts.find((d) => /^H_/i.test(d.counterpartCode))?.counterpartCode.trim().toLowerCase(); const tCode = drafts.find((d) => /^T_/i.test(d.product_code))?.product_code?.trim().toLowerCase() || productSetupDrafts.find((d) => /^T_/i.test(d.counterpartCode))?.counterpartCode.trim().toLowerCase(); const hProduct = hCode ? productByCode.get(hCode) : undefined; const tProduct = tCode ? productByCode.get(tCode) : undefined; if (!primary && !hProduct && !tProduct) throw new Error("Produkt se nepodařilo dohledat.");
    const records = selected.map((r) => { const product = (r.position === "TUP" ? tProduct : hProduct) || primary; if (!product) throw new Error(`Pro pozici ${r.position} nebyl nalezen Product ID.`); return { employee_id: r.employeeId, work_date: workDate, shift, line: line.trim(), product_id: product.id, position: r.position, oee: Number(r.oee) || 0, performance: Number(r.performance) || 0, available_time: Number(r.availableTime) || 0, help_score: Number(r.helpScore) || 0, screenshot_path: screenshotPath, ...approval() }; }); const { error } = await supabase.from("daily_records").insert(records); if (error) throw error; return selected.length; }, onSuccess: (count) => { qc.invalidateQueries(); toast.success(`Importováno ${count} řádků.`); onImported?.(); reset(); }, onError: (e: Error) => toast.error(`Import se nepodařil: ${e.message}`) });
  const otherLineNotices = useMemo(() => { if (!workDate || !line.trim()) return [] as { name: string; lines: string[] }[]; const l = line.trim().toLowerCase(); return rows.filter((r) => r.include && r.employeeId).map((r) => { const agg = existingShifts.find((a) => a.employee_id === r.employeeId && a.work_date === workDate && a.shift === shift); if (!agg) return null; const otherLines = agg.lines.filter((x) => x.trim().toLowerCase() !== l); if (!otherLines.length) return null; const name = employees.find((e) => e.id === r.employeeId)?.full_name ?? r.ocrName; return { name, lines: otherLines }; }).filter((x): x is { name: string; lines: string[] } => x !== null); }, [workDate, line, rows, existingShifts]); const duplicateNames = useMemo(() => { if (!workDate || !line.trim()) return []; const l = line.trim().toLowerCase(); return rows.filter((r) => r.include && r.employeeId && existingRecords.some((x) => x.employee_id === r.employeeId && x.work_date === workDate && x.shift === shift && x.line.trim().toLowerCase() === l)).map((r) => employees.find((e) => e.id === r.employeeId)?.full_name ?? r.ocrName); }, [workDate, line, rows, existingRecords, employees]); const lowConf = (c: number) => c < 0.7; const importBlocked = stage !== "ready" || missingProductCodes.length > 0;
  return <Card className="min-w-0 gap-4 overflow-hidden p-4 shadow-[var(--shadow-card)] sm:p-5"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="text-sm font-semibold uppercase tracking-wide text-muted-foreground">Import ze screenshotu</h2>{result ? <Button variant="ghost" size="sm" onClick={reset}><X className="h-4 w-4" /> Zrušit</Button> : null}</div><input ref={fileRef} type="file" accept="image/*" className="hidden" onChange={(e) => { const f = e.target.files?.[0]; if (f) void onFile(f); }} />{!result ? <div className="grid gap-2"><Button size="lg" className="h-12 w-full" disabled={busy} onClick={() => fileRef.current?.click()}>{busy ? <><Loader2 className="h-4 w-4 animate-spin" /> Rozpoznávám…</> : <><ImageUp className="h-4 w-4" /> Nahrát screenshot</>}</Button><p className="text-xs text-muted-foreground">Data se nikdy neuloží automaticky – nejprve zobrazíme návrh k odsouhlasení. Ruční zadávání zůstává beze změny.</p></div> : <div className="grid min-w-0 gap-4"><div className="rounded-md border border-primary/40 bg-primary/5 px-3 py-2 text-sm"><strong>{stage === "products" ? "1/3 Produkty" : stage === "employees" ? "2/3 Zaměstnanci" : stage === "hourly" ? "3/3 Hodinová data" : "Import připraven"}</strong>{" – "}{stage === "products" ? "Nejprve se porovnávají Product ID s databází." : stage === "employees" ? "Po vyřešení zaměstnanců se načtou hodinová data." : stage === "hourly" ? "Probíhá načtení hodinových dat a výpočet skutečného OEE z normy, kapacity a počtu operátorů." : "Zkontrolujte všechna rozpoznaná data před uložením."}</div><div className="rounded-md border border-warning/40 bg-warning/10 px-3 py-2 text-sm"><strong>Zkontrolujte importovaná data.</strong> Zvýrazněné hodnoty jsou nejisté nebo chybí.</div>{productDrafts.length > 0 ? <div className="rounded-md border border-primary/40 bg-primary/5 p-3"><div className="mb-2 text-sm font-semibold">Rozpoznané Product ID</div><div className="grid gap-2">{productDrafts.map((p) => { const existing = findProductByCode(allProducts, p.product_code); const operation: "HA" | "TUP" = /^T_/i.test(p.product_code) ? "TUP" : "HA"; const dbNorm = existing ? currentNorm(norms, existing.id, operation)?.norm_per_hour : null; const profile = productProfiles.find((candidate) => profileMatchesCode(candidate, p.product_code)); const profileNorm = profile ? profileNormForCode(profile, p.product_code) : null; const importNorm = profileNorm ?? dbNorm ?? p.norm_per_hour; const setup = productSetupDrafts.find((d) => d.code.trim().toLowerCase() === p.product_code.trim().toLowerCase()); return <div key={p.key} className="rounded border p-3"><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><div><div className="font-medium">{p.product_code}</div><div className="text-[11px] text-muted-foreground">jistota {Math.round(p.confidence * 100)} % · {existing ? "Product ID evidováno" : "nové Product ID"}</div></div>{existing ? <span className="text-xs text-muted-foreground">norma z Product Profile</span> : <span className="text-xs text-warning">nutno založit</span>}</div>{existing ? <div className="text-sm font-medium">{importNorm != null ? `${importNorm} ks/h` : "norma chybí"}</div> : setup ? <div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-1.5"><Label>Norma (ks/h) *</Label><Input type="number" min="0.1" step="0.1" inputMode="decimal" value={setup.norm} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === setup.key ? { ...x, norm: e.target.value } : x))} placeholder="např. 100" /></div><div className="grid gap-1.5"><Label>Kapacita / operátoři *</Label><Input type="number" min="1" step="1" inputMode="numeric" value={setup.capacity} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === setup.key ? { ...x, capacity: e.target.value } : x))} /></div></div> : null}</div>; })}</div>{missingProductCodes.length > 0 ? <div className="mt-3 flex flex-wrap items-center justify-between gap-2 rounded-md border border-warning/40 bg-warning/10 p-3"><span className="text-sm">Nové Product ID vyžadují potvrzení normy a kapacity.</span><Button type="button" disabled={productSetupSaving || !productSetupDrafts.length} onClick={() => void createProductIds()}><Check className="h-4 w-4" /> {productSetupSaving ? "Vytvářím…" : `Založit ${missingProductCodes.length} Product ID a pokračovat`}</Button></div> : null}</div> : null}<div className="grid grid-cols-1 gap-3 md:grid-cols-4"><div className="grid gap-1"><Label>Datum</Label><Input type="date" value={workDate} onChange={(e) => setWorkDate(e.target.value)} /></div><div className="grid gap-1"><Label>Směna</Label><Select value={shift} onValueChange={setShift}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{SHIFTS.map((s) => <SelectItem key={s} value={s}>{s}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-1"><Label>Linka</Label><Input value={line} onChange={(e) => setLine(e.target.value)} placeholder="např. L1" /></div><div className="grid gap-1"><Label>Kód produktu</Label><Input value={productCode} onChange={(e) => setProductCode(e.target.value)} /></div></div>{(duplicateNames.length || otherLineNotices.length) ? <div className="grid gap-2">{duplicateNames.length ? <div className="rounded-md border border-destructive/40 bg-destructive/5 px-3 py-2 text-sm"><strong>Duplicitní řádky:</strong> {duplicateNames.join(", ")}</div> : null}{otherLineNotices.length ? <div className="rounded-md border border-warning/40 bg-warning/10 px-3 py-2 text-sm"><strong>Upozornění:</strong> {otherLineNotices.map((x) => `${x.name}: ${x.lines.join(", ")}`).join("; ")}</div> : null}</div> : null}<div className="overflow-x-auto rounded-md border"><table className="w-full min-w-[900px] text-sm"><thead><tr className="border-b bg-muted/50"><th className="p-2 text-left">Zahrnout</th><th className="p-2 text-left">Zaměstnanec</th><th className="p-2 text-left">Pozice</th><th className="p-2 text-left">OEE</th><th className="p-2 text-left">Výkon</th><th className="p-2 text-left">Dostupný čas</th><th className="p-2 text-left">Pomoc</th><th className="p-2 text-left">Jistota</th><th className="p-2 text-left"></th></tr></thead><tbody>{rows.map((r) => <tr key={r.key} className="border-b last:border-0"><td className="p-2"><input type="checkbox" checked={r.include} onChange={(e) => patch(r.key, { include: e.target.checked })} /></td><td className="p-2"><div className="flex items-center gap-2"><Select value={r.employeeId ?? "__none"} onValueChange={(v) => patch(r.key, { employeeId: v === "__none" ? null : v })}><SelectTrigger className="min-w-[220px]"><SelectValue placeholder={r.ocrName || "Vyberte zaměstnance"} /></SelectTrigger><SelectContent><SelectItem value="__none">{r.ocrName || "Nenalezeno"}</SelectItem>{activeEmployees.map((e) => <SelectItem key={e.id} value={e.id}>{e.full_name}{e.personal_no ? ` (${e.personal_no})` : ""}</SelectItem>)}</SelectContent></Select><Button type="button" variant="outline" size="sm" onClick={() => openAddEmployee(r)}><Plus className="h-4 w-4" /></Button></div></td><td className="p-2"><Select value={r.position} onValueChange={(v) => patch(r.key, { position: v as "HA" | "TUP" })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="HA">HA</SelectItem><SelectItem value="TUP">TUP</SelectItem></SelectContent></Select></td><td className="p-2"><Input className={lowConf(r.confidence) ? "border-warning" : ""} value={r.oee} onChange={(e) => patch(r.key, { oee: e.target.value })} /></td><td className="p-2"><Input className={lowConf(r.confidence) ? "border-warning" : ""} value={r.performance} onChange={(e) => patch(r.key, { performance: e.target.value })} /></td><td className="p-2"><Input className={lowConf(r.confidence) ? "border-warning" : ""} value={r.availableTime} onChange={(e) => patch(r.key, { availableTime: e.target.value })} /></td><td className="p-2"><Input value={r.helpScore} onChange={(e) => patch(r.key, { helpScore: e.target.value })} /></td><td className="p-2">{Math.round(r.confidence * 100)} %</td><td className="p-2"><Button variant="ghost" size="sm" onClick={() => patch(r.key, { include: false })}><Trash2 className="h-4 w-4" /></Button></td></tr>)}</tbody></table></div><div className="flex flex-wrap justify-end gap-2">{stage === "employees" ? <Button variant="secondary" disabled={busy || unresolvedEmployeeRows.length > 0 || !rows.length} onClick={() => void continueAfterEmployees()}>Pokračovat na hodinová data</Button> : null}<Button variant="outline" onClick={reset}>Zrušit</Button><Button disabled={confirmImport.isPending || importBlocked || !rows.some((r) => r.include && r.employeeId)} onClick={() => confirmImport.mutate()}>{confirmImport.isPending ? <Loader2 className="h-4 w-4 animate-spin" /> : <Check className="h-4 w-4" />} Potvrdit import</Button></div></div>}
    <Dialog open={employeeSetupOpen} onOpenChange={(open) => { if (!busy && !createEmployee.isPending) setEmployeeSetupOpen(open); }}><DialogContent className="max-h-[90vh] w-[calc(100%-2rem)] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle>2/3 – Zaměstnanci z OCR</DialogTitle></DialogHeader><div className="grid gap-3"><div className="rounded-md border bg-muted/30 px-3 py-2 text-sm text-muted-foreground">OCR přečetl pracovníky až po vyřešení Product ID. Existující zaměstnanci jsou přiřazeni automaticky. Nenalezené můžete vytvořit tlačítkem +.</div><div className="overflow-x-auto rounded-md border"><table className="w-full min-w-[620px] text-sm"><thead><tr className="border-b bg-muted/50"><th className="p-2 text-left">OCR jméno</th><th className="p-2 text-left">Stav</th><th className="p-2 text-left">Akce</th></tr></thead><tbody>{rows.map((r) => <tr key={r.key} className="border-b last:border-0"><td className="p-2">{r.ocrName || "Jméno nebylo rozpoznáno"}</td><td className="p-2">{r.employeeId ? "✓ Evidován" : "Nenalezen"}</td><td className="p-2">{r.employeeId ? <span className="text-xs text-muted-foreground">Přiřazen automaticky</span> : <Button type="button" size="sm" variant="outline" onClick={() => openAddEmployee(r)}><Plus className="h-4 w-4" /> Vytvořit / přiřadit</Button>}</td></tr>)}</tbody></table></div><DialogFooter><Button variant="outline" onClick={() => setEmployeeSetupOpen(false)}>Zrušit</Button><Button disabled={busy || unresolvedEmployeeRows.length > 0} onClick={() => void continueAfterEmployees()}>{busy ? <><Loader2 className="h-4 w-4 animate-spin" /> Načítám…</> : <>Pokračovat na hodinová data</>}</Button></DialogFooter></div></DialogContent></Dialog>
    <Dialog open={addEmployeeOpen} onOpenChange={(open) => { if (!createEmployee.isPending) { setAddEmployeeOpen(open); if (!open) { setAddEmployeeRowKey(null); setAddEmployeeName(""); setAddEmployeePersonalNo(""); } } }}><DialogContent><DialogHeader><DialogTitle>Přidat zaměstnance k importu</DialogTitle></DialogHeader><div className="grid gap-3"><div className="rounded-md border bg-muted/30 px-3 py-2 text-sm text-muted-foreground">Jméno z OCR je předvyplněné. Po uložení bude nový zaměstnanec automaticky přiřazen k tomuto řádku importu.</div><div className="grid gap-1"><Label>Jméno a příjmení <span className="text-destructive">*</span></Label><Input value={addEmployeeName} onChange={(e) => setAddEmployeeName(e.target.value)} autoFocus disabled={createEmployee.isPending} /></div><div className="grid gap-1"><Label>Osobní číslo</Label><Input value={addEmployeePersonalNo} onChange={(e) => setAddEmployeePersonalNo(e.target.value)} disabled={createEmployee.isPending} /></div></div><DialogFooter><Button type="button" variant="outline" disabled={createEmployee.isPending} onClick={() => setAddEmployeeOpen(false)}>Zrušit</Button><Button type="button" disabled={createEmployee.isPending || !addEmployeeName.trim()} onClick={() => createEmployee.mutate()} children="Vytvořit a přiřadit" /></DialogFooter></DialogContent></Dialog>
    <Dialog open={newProductModalOpen} onOpenChange={(open) => { if (!productSetupSaving) setNewProductModalOpen(open); }}><DialogContent className="max-h-[90vh] w-[calc(100%-2rem)] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle>1/3 – Kontrola / založení Product ID</DialogTitle></DialogHeader><div className="grid gap-4"><div className="rounded-xl border bg-muted/20 p-4 text-sm"><div className="font-semibold">OCR našel nové Product ID</div><div className="mt-1 text-muted-foreground">H_ a T_ jsou varianty jednoho Product ID. Kód druhé varianty se NEODVOZUJE automaticky. Pokud protějšek v OCR není, zadejte jeho skutečný kód ručně. H_ a T_ mohou mít odlišný název, normu i kapacitu.</div></div><div className="grid gap-1.5"><Label>Název Product ID *</Label><Input value={productIdName} onChange={(e) => setProductIdName(e.target.value)} placeholder="Zadejte vlastní název Product ID" autoFocus /><p className="text-xs text-muted-foreground">Název Product ID zadáváte ručně; OCR ho nenastavuje.</p></div><div className="grid gap-3">{productSetupDrafts.map((draft) => { const isH = /^H_/i.test(draft.code); const opposite = isH ? "T_" : "H_"; const counterpartExists = draft.counterpartCode.trim() ? Boolean(findProductByCode(allProducts, draft.counterpartCode)) : false; return <div key={draft.key} className="rounded-lg border p-4"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><div><div className="font-semibold">{draft.code}</div><div className="text-xs text-muted-foreground">OCR jistota {Math.round(draft.confidence * 100)} %</div></div><span className="text-xs text-warning">rozpoznaná varianta</span></div><div className="grid gap-3 sm:grid-cols-3"><div className="grid gap-1.5 sm:col-span-2"><Label>Název varianty *</Label><Input value={draft.name} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, name: e.target.value } : x))} placeholder={isH ? "Vlastní název H_ varianty" : "Vlastní název T_ varianty"} /></div><div className="grid gap-1.5"><Label>Kapacita / operátoři *</Label><Input type="number" min="1" step="1" inputMode="numeric" value={draft.capacity} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, capacity: e.target.value } : x))} /></div><div className="grid gap-1.5 sm:col-span-3"><Label>Norma (ks/h) *</Label><Input type="number" min="0.1" step="0.1" inputMode="decimal" value={draft.norm} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, norm: e.target.value } : x))} placeholder="např. 100" /></div></div><div className="mt-4 rounded-md border border-dashed bg-muted/20 p-3"><div className="mb-2 text-sm font-semibold">Protějšek {opposite}_ – zadává se pouze ručně</div><div className="grid gap-3 sm:grid-cols-2"><div className="grid gap-1.5"><Label>Kód protějšku</Label><Input value={draft.counterpartCode} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, counterpartCode: e.target.value } : x))} placeholder={`např. ${opposite}...`} /></div><div className="grid gap-1.5"><Label>Název protějšku</Label><Input value={draft.counterpartName} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, counterpartName: e.target.value } : x))} placeholder="Vlastní název protějšku" /></div><div className="grid gap-1.5"><Label>Norma protějšku</Label><Input type="number" min="0.1" step="0.1" value={draft.counterpartNorm} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, counterpartNorm: e.target.value } : x))} placeholder="např. 80" /></div><div className="grid gap-1.5"><Label>Kapacita protějšku</Label><Input type="number" min="1" step="1" value={draft.counterpartCapacity} onChange={(e) => setProductSetupDrafts((prev) => prev.map((x) => x.key === draft.key ? { ...x, counterpartCapacity: e.target.value } : x))} placeholder="např. 1" /></div></div>{draft.counterpartCode.trim() ? <p className="mt-2 text-xs text-muted-foreground">{counterpartExists ? "Protějšek už v databázi existuje – nebude znovu založen." : "Protějšek bude založen pouze s ručně zadanými údaji."}</p> : <p className="mt-2 text-xs text-muted-foreground">Protějšek není povinný. Program ho nevytváří automaticky.</p>}</div></div>})}</div></div><DialogFooter><Button variant="outline" disabled={productSetupSaving} onClick={() => setNewProductModalOpen(false)}>Zrušit</Button><Button disabled={productSetupSaving || !productSetupDrafts.length || !productIdName.trim()} onClick={async () => { try { await createProductIds(); } catch (e) { toast.error((e as Error).message); } }}><Check className="h-4 w-4" /> {productSetupSaving ? "Vytvářím…" : "Založit položky a pokračovat"}</Button></DialogFooter></DialogContent></Dialog>
  </Card>;
}
